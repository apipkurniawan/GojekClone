import React, {Component} from 'react';
import {Text, View} from 'react-native';

class Orders extends Component {
  render() {
    return (
      <View style={{flex: 1}}>
        <View style={{flex: 1}}>
          <Text>ini halaman Orders</Text>
        </View>
      </View>
    );
  }
}

export default Orders;
